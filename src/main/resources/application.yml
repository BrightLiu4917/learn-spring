# Spring ????
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql:// 10.123.234.38:3306/learn-spring?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: root
    # ?????
    hikari:
      minimum-idle: 5
      maximum-pool-size: 10
      connection-timeout: 30000
      max-lifetime: 1800000
      connection-test-query: SELECT 1

# MyBatis-Plus ??
mybatis-plus:
  # Mapper XML ??????? XML ? SQL ????
  mapper-locations: classpath:mapper/**/*.xml
  # ????????????????????
  type-aliases-package: com.liuweiliang.demo1.entity
  # ????
  global-config:
    db-config:
      # ??????????????ASSIGN_ID????AUTO?
      id-type: ASSIGN_ID
      # ???????????? t_??????????????
      # table-prefix: t_
  # ???
  configuration:
    # ???????????? user_name ? userName?
    map-underscore-to-camel-case: true
    # ?? SQL ???????????????????
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    # ?????????????????
    cache-enabled: false
    # ??????????? null ??????
    call-setters-on-nulls: true

# ?????????? SQL ???
logging:
  level:
    # ??????? SQL ???????? Mapper ????
    com.liuweiliang.demo1.mapper: debug


# ========== Swagger?SpringDoc-OpenAPI??? ==========
springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html  # ??????
    operationsSorter: method  # ????????
  packages-to-scan: com.liuweiliang.demo1.controller  # ????Controller?

# ?????????
openapi:
  info:
    title: Demo1??????
    version: 0.0.1-SNAPSHOT
    description: ??MyBatis-Plus/Druid/Swagger?Spring Boot 4.0.1??????
    contact:
      name: ???
      email: your-email@example.com